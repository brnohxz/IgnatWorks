import {initialPeopleObject} from "../HW8";

export const homeWorkReducer = (state: initialPeopleObject[], action: any): any => { // need to fix any
    switch (action.type) {
        case 'sort': {
            // need to fix
            return state
        }
        case 'check': {
            // need to fix
            return state
        }
        default: return state
    }
}